<%= content_for :title, 'Главная' %>



<div class="profile-panel bit-content orange-gray">
  <div class="profile-panel-left bit-content orange-gray">
    <div style="width: 100%; height: 50px; text-align: center; margin: 5px">
      <h3>Меню Шаблоны</h3>
    </div>

    <div class="group main">
      <%= link_to t('.heroes', :default => t("helpers.links.heroes")), character_templates_path(), :class => 'btn btn-bit btn-full green' %>
      <br>
    </div>
    <br>
    <div class="group main">
      <%= link_to t('helpers.links.quests', :default => t('helpers.links.quests')), quest_templates_path(), :class => 'btn btn-bit btn-full green' %>
      <br>
      <%= link_to t('helpers.links.rewards', :default => t('helpers.links.rewards')), reward_templates_path(), :class => 'btn btn-bit btn-full green' %>
      <br>
      <%= link_to t('helpers.links.monsters', :default => t("helpers.links.monsters")), monsters_path(), :class => 'btn btn-bit btn-full green' %>
    </div>
    <div class="group help">
      <%= link_to t('helpers.links.options', :default => t("helpers.links.options")), character_templates_path(), :class => 'btn btn-bit btn-full outline' %>
      <br>
      <%= link_to t('helpers.links.help', :default => t("helpers.links.help")), character_templates_path(), :class => 'btn btn-bit btn-full outline' %>
    </div>
  </div>


</div>
