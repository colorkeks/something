<%= simple_form_for(@armor_template) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :type %>
    <%= f.input :percent %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
